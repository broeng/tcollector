# Should the collector even attempt to collect?
enabled = True

# How often should we collect?
poll_interval = 60

# Prefix for collected metrics
prefix = "lsof"

# What file systems to run on
filesystems = ["/"]
